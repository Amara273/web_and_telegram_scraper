def handleChannel(channel):
    return (
            channel.id,
            str(channel.title),
            str(channel.photo),
            str(channel.date),
            channel.creator,
            channel.left,
            channel.broadcast,
            channel.verified,
            channel.megagroup,
            channel.restricted,
            channel.signatures,
            channel.min,
            channel.scam,
            channel.has_link,
            channel.has_geo,
            channel.slowmode_enabled,
            channel.call_active,
            channel.call_not_empty,
            channel.fake,
            channel.gigagroup,
            channel.noforwards,
            channel.join_to_send,
            channel.join_request,
            channel.forum,
            channel.stories_hidden,
            channel.stories_hidden_min,
            channel.stories_unavailable,
            channel.signature_profiles,
            channel.autotranslation,
            channel.broadcast_messages_allowed,
            channel.monoforum,
            channel.forum_tabs,
            channel.access_hash,
            channel.username,
            str(channel.restriction_reason),
            str(channel.admin_rights),
            str(channel.banned_rights),
            str(channel.default_banned_rights),
            channel.participants_count,
            str(channel.usernames),
            str(channel.stories_max_id),
            str(channel.color),
            str(channel.profile_color),
            str(channel.emoji_status),
            str(channel.level),
            str(channel.subscription_until_date),
            str(channel.bot_verification_icon),
            str(channel.send_paid_messages_stars),
            str(channel.linked_monoforum_id)
        )
